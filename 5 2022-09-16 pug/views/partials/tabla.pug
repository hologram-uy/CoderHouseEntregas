if(productos.length !== 0) 
    <div class="table-responsive">
        <table class="table table-dark">
            tr(style="color: yellow;") 
                th=Título 
                th=Precio 
                th=Url
                for prod in productos
                    tr
                        td=prod.title
                        td=prod.price
                        td=thumb 
                            img(src=prod.thumbnail width=80 height=80)
        </table>
    </div>
else
    <h3 style="color: black">No hay productos.</h3>


