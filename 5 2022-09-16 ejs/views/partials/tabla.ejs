<% if(contenido > 0) { %>
    <div class="table-responsive">
        <table class="table table-dark">
            <tr style="color: yellow;"> <th>Título</th> <th>Precio</th> <th>Url</th> </tr>
           <% for (const prod in productos ) { %>
            <tr>
                <td> <%= productos[prod].title %> </td>
                <td> <%= productos[prod].price %> </td>
                <td> <img src="<%= productos[prod].thumbnail %>" width="80" height="80" /></td>
            </tr>
            <% } %>
        </table>
    </div>
<% } else{ %> 
   <h3 style="color: black">No hay productos.</h3>
<% }%>

